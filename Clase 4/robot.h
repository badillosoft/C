#ifndef ROBOT_H // Comprobamos que la libreria no este definida

#define ROBOT_H // En tal caso definimos la libreria

struct Robot {
    char name[10];
    double x;
    double y;
};

#endif // Fin de la comprobacion