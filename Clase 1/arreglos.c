#include <stdio.h>

int main(int argc, char *argv[]) {
	
	// Creamos un arreglo de 10 enteros llamado datos
	int datos[10];

	// Asignamos el dato 0 a 32	
	datos[0] = 1;
	
	printf("datos[0] = %i", datos[0]);
	
	return 0;
}